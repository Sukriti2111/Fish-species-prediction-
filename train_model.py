# -*- coding: utf-8 -*-
"""train_model.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1Enfd85oXl29PsRLxFvx7pjtRIWd9SHw1
"""

import pandas as pd

# Load the dataset
df = pd.read_csv('Fish.csv')
print(df.head())

from sklearn.preprocessing import LabelEncoder, StandardScaler

# Encode categorical variables
le = LabelEncoder()
df['Species'] = le.fit_transform(df['Species'])

# Scale the data
scaler = StandardScaler()
features = df.drop(['Species'], axis=1)
features_scaled = scaler.fit_transform(features)

from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import accuracy_score

# Split the data
X_train, X_test, y_train, y_test = train_test_split(features_scaled, df['Species'], test_size=0.2, random_state=42)

# Train the model
clf = RandomForestClassifier(n_estimators=100, random_state=42)
clf.fit(X_train, y_train)

# Predict and evaluate
y_pred = clf.predict(X_test)
print('Accuracy:', accuracy_score(y_test, y_pred))

import joblib

# Save the model
joblib.dump(clf, 'fish_model.pkl')

import sklearn
print(sklearn.__version__)

